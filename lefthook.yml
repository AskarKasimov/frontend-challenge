pre-commit:
  parallel: true
  commands:
    biome:
      root: catty-frontend
      glob: "**/*.{js,ts,jsx,tsx,json}"
      run: yarn lint {staged_files}
      stage_fixed: true
    typecheck:
      root: catty-frontend
      run: yarn typecheck
      stage_fixed: false
